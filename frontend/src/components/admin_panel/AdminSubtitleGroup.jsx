const AdminSubtitleGroup = ({ subTitle }) => {
  return (
    <div className="admin-panel-toolbar-item">
      <span className="admin-panel-subtitle">{subTitle}</span>
    </div>
  );
};

export default AdminSubtitleGroup;
